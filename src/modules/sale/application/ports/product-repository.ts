// MIGRATION-TODO: substituir pelo repositório do módulo Catalog quando for criado.
import type { ProductRepository as BaseProductRepository } from '@/repositories/product-repository'

export type ProductRepository = BaseProductRepository
